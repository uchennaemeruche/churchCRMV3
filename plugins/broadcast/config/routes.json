{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "broadcast.index",
      "config": {
        "policies": []
      }
    },
     {
      "method": "POST",
      "path": "/",
      "handler": "broadcast.send",
      "config": {
        "policies": [],
        "validate": "plugins.broadcast.config.validators.broadcast"
      }
    }
  ]
}
